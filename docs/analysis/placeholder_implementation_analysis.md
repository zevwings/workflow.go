# Placeholder 实现可行性分析

## 概述

分析是否可以在不引入 `huh`/`bubbletea` 的情况下，基于现有代码实现 placeholder 功能。

---

## 1. 现有技术基础

### 1.1 已有的字符级输入实现

当前代码库中已经有一个完整的字符级输入实现：`readPasswordInput` 函数。

**关键特性**：
- ✅ 使用 `golang.org/x/term.MakeRaw()` 设置原始模式
- ✅ 字符级读取（`os.Stdin.Read(buf[:])`）
- ✅ 处理退格键、回车键、Ctrl+C
- ✅ 使用 ANSI 转义码控制光标（`\b \b`）
- ✅ 实时显示反馈（显示 `*` 字符）

**代码位置**：`internal/lib/prompt/input.go:135-200`

### 1.2 可复用的技术点

```go
// 1. 设置原始模式（已实现）
fd := int(os.Stdin.Fd())
oldState, err := term.MakeRaw(fd)
defer term.Restore(fd, oldState)

// 2. 字符级读取（已实现）
var buf [1]byte
os.Stdin.Read(buf[:])

// 3. 光标控制（已实现）
fmt.Print("\b \b")  // 退格并清除字符

// 4. 字符处理逻辑（已实现）
// - 回车键：结束输入
// - 退格键：删除字符
// - Ctrl+C：取消输入
```

---

## 2. 实现方案设计

### 2.1 核心思路

基于 `readPasswordInput` 的实现，创建一个新的 `readInputWithPlaceholder` 函数：

1. **复用字符级读取逻辑**
2. **修改显示逻辑**：
   - 如果输入为空 → 显示 placeholder（灰色）
   - 如果输入不为空 → 显示实际字符
3. **处理 placeholder 的清除**：
   - 用户输入第一个字符时，清除 placeholder
   - 用户删除所有字符时，重新显示 placeholder

### 2.2 实现伪代码

```go
func readInputWithPlaceholder(promptText, placeholder string) (string, error) {
    fd := int(os.Stdin.Fd())
    oldState, err := term.MakeRaw(fd)
    if err != nil {
        return readInputFallback()
    }
    defer term.Restore(fd, oldState)

    var value []byte
    var buf [1]byte
    hasPlaceholder := true  // 是否正在显示 placeholder

    // 初始显示 placeholder
    if placeholder != "" {
        fmt.Print(formatPlaceholder(placeholder))
        hasPlaceholder = true
    }

    for {
        n, err := os.Stdin.Read(buf[:])
        if err != nil || n == 0 {
            // 处理结束
            break
        }

        char := buf[0]

        // 处理回车键
        if char == '\r' || char == '\n' {
            fmt.Println()
            break
        }

        // 处理退格键
        if char == 127 || char == 8 {
            if len(value) > 0 {
                // 删除字符
                value = value[:len(value)-1]

                if len(value) == 0 && placeholder != "" {
                    // 如果删除到空，重新显示 placeholder
                    fmt.Print("\b \b")  // 清除最后一个字符
                    fmt.Print(formatPlaceholder(placeholder))
                    hasPlaceholder = true
                } else {
                    // 正常退格
                    fmt.Print("\b \b")
                }
            } else if hasPlaceholder {
                // 如果正在显示 placeholder，退格应该清除 placeholder
                clearPlaceholder(placeholder)
                hasPlaceholder = false
            }
            continue
        }

        // 处理 Ctrl+C
        if char == 3 {
            term.Restore(fd, oldState)
            fmt.Println()
            return "", fmt.Errorf("用户取消输入")
        }

        // 跳过控制字符
        if char < 32 {
            continue
        }

        // 处理普通字符
        if hasPlaceholder {
            // 清除 placeholder
            clearPlaceholder(placeholder)
            hasPlaceholder = false
        }

        value = append(value, char)
        fmt.Print(string(char))  // 显示实际字符
    }

    term.Restore(fd, oldState)
    return strings.TrimSpace(string(value)), nil
}
```

### 2.3 关键技术点

#### 2.3.1 Placeholder 的显示和清除

**显示 placeholder**：
```go
func formatPlaceholder(text string) string {
    theme := GetTheme()
    if !theme.EnableColor {
        return text
    }
    // 使用灰色显示
    return theme.HintColor(text)  // 或使用新的 PlaceholderColor
}
```

**清除 placeholder**：
```go
func clearPlaceholder(placeholder string) {
    // 方法1：逐个字符退格
    for i := 0; i < len(placeholder); i++ {
        fmt.Print("\b \b")
    }

    // 方法2：使用 ANSI 转义码（更高效）
    // \r: 回到行首
    // \033[K: 清除到行尾
    fmt.Print("\r")
    fmt.Print("\033[K")
}
```

#### 2.3.2 光标位置管理

**挑战**：
- 需要知道当前光标位置
- 需要正确清除 placeholder
- 需要处理多字节字符（UTF-8）

**解决方案**：
- 使用 `\r` + `\033[K` 清除整行（更简单）
- 或者计算 placeholder 的显示宽度（考虑多字节字符）

---

## 3. 技术可行性分析

### 3.1 ✅ 完全可行的部分

1. **字符级读取**：
   - ✅ 已有实现（`readPasswordInput`）
   - ✅ 技术成熟，稳定可靠

2. **终端控制**：
   - ✅ 已有 `golang.org/x/term` 依赖
   - ✅ ANSI 转义码支持完善

3. **样式渲染**：
   - ✅ 已有 `formatPlaceholder` 类似的函数（`formatHint`）
   - ✅ 主题系统已支持颜色控制

### 3.2 ⚠️ 需要注意的部分

1. **多字节字符处理**：
   - **问题**：UTF-8 字符可能占用多个字节
   - **影响**：计算 placeholder 宽度时可能不准确
   - **解决方案**：
     - 使用 `unicode/utf8` 包计算字符数
     - 或者使用 `\r` + `\033[K` 清除整行（更简单）

2. **终端兼容性**：
   - **问题**：不同终端对 ANSI 转义码的支持可能不同
   - **影响**：某些终端可能显示异常
   - **解决方案**：
     - 检测终端能力（`term.IsTerminal`）
     - 提供回退方案（类似 `readPasswordFallback`）

3. **错误处理**：
   - **问题**：如果 `MakeRaw` 失败，需要回退
   - **影响**：某些环境可能无法使用 placeholder
   - **解决方案**：
     - 已有 `readPasswordFallback` 模式
     - 可以回退到普通输入（不显示 placeholder）

### 3.3 🔍 需要验证的部分

1. **光标位置同步**：
   - 需要验证：清除 placeholder 后，光标位置是否正确
   - 测试场景：不同长度的 placeholder

2. **退格键行为**：
   - 需要验证：在显示 placeholder 时按退格键的行为
   - 预期：应该清除 placeholder，而不是删除字符

3. **性能影响**：
   - 需要验证：字符级读取是否会影响性能
   - 预期：影响很小，因为只是本地输入

---

## 4. 实现复杂度评估

### 4.1 代码量估算

- **新函数**：`readInputWithPlaceholder` - 约 100-150 行
- **辅助函数**：
  - `formatPlaceholder` - 约 10 行
  - `clearPlaceholder` - 约 10 行
- **修改现有代码**：
  - `input` 函数 - 添加 placeholder 参数和调用 - 约 20 行
  - `InputBuilder` - 添加 `Placeholder` 方法 - 约 10 行

**总计**：约 150-200 行代码

### 4.2 实现难度

- **难度等级**：⭐⭐⭐（中等）
- **原因**：
  - 可以复用现有代码（`readPasswordInput`）
  - 主要是修改显示逻辑
  - 需要处理一些边界情况

### 4.3 测试复杂度

- **测试场景**：
  1. 基本功能：显示 placeholder，输入字符，清除 placeholder
  2. 退格键：在 placeholder 状态下按退格键
  3. 空输入：直接回车（placeholder 应该消失）
  4. 多字节字符：placeholder 包含中文、emoji 等
  5. 终端兼容性：不同终端环境

---

## 5. 与 huh/bubbletea 的对比

### 5.1 功能对比

| 特性 | huh/bubbletea | 我们的实现 |
|------|---------------|-----------|
| **Placeholder 显示** | ✅ | ✅ |
| **实时更新** | ✅ | ✅ |
| **样式支持** | ✅（lipgloss） | ✅（fatih/color） |
| **光标闪烁** | ✅ | ❌（可添加） |
| **多行输入** | ✅ | ❌（单行） |
| **依赖** | bubbletea + lipgloss | golang.org/x/term（已有） |

### 5.2 架构对比

**huh/bubbletea**：
- 基于事件循环的 TUI 架构
- 完整的组件系统
- 支持复杂的界面布局

**我们的实现**：
- 基于字符级读取的简单实现
- 专注于输入功能
- 轻量级，无额外依赖

### 5.3 优势

**我们的实现优势**：
1. ✅ **零额外依赖**：只使用已有的 `golang.org/x/term`
2. ✅ **简单直接**：不需要理解 TUI 架构
3. ✅ **轻量级**：代码量少，性能开销小
4. ✅ **易于维护**：基于现有代码，逻辑清晰

**huh/bubbletea 的优势**：
1. ✅ **功能完整**：支持更多高级特性
2. ✅ **社区支持**：有活跃的社区和维护
3. ✅ **文档完善**：有详细的文档和示例

---

## 6. 实现步骤建议

### 6.1 第一阶段：基础实现

1. **创建 `readInputWithPlaceholder` 函数**
   - 基于 `readPasswordInput` 修改
   - 实现基本的 placeholder 显示和清除

2. **添加 `formatPlaceholder` 函数**
   - 使用主题系统格式化 placeholder

3. **修改 `input` 函数**
   - 添加 `placeholder` 参数
   - 在非密码模式下调用新函数

### 6.2 第二阶段：完善功能

1. **处理边界情况**
   - 多字节字符
   - 终端兼容性
   - 错误处理

2. **添加 Builder API 支持**
   - `InputBuilder.Placeholder()` 方法

3. **测试和优化**
   - 各种场景测试
   - 性能优化

### 6.3 第三阶段：可选增强

1. **光标闪烁**（可选）
   - 使用 ANSI 转义码实现光标闪烁

2. **更丰富的样式**（可选）
   - 支持更多 placeholder 样式选项

---

## 7. 潜在问题和解决方案

### 7.1 问题：Placeholder 清除不干净

**现象**：清除 placeholder 后，终端显示异常

**原因**：多字节字符宽度计算不准确

**解决方案**：
```go
// 使用整行清除，而不是逐个字符退格
func clearPlaceholder(placeholder string) {
    fmt.Print("\r")      // 回到行首
    fmt.Print("\033[K")  // 清除到行尾
}
```

### 7.2 问题：在 placeholder 状态下按退格键

**现象**：行为不明确

**预期行为**：
- 如果正在显示 placeholder → 清除 placeholder，不删除字符
- 如果已有输入 → 正常删除字符

**解决方案**：
```go
if hasPlaceholder {
    clearPlaceholder(placeholder)
    hasPlaceholder = false
    continue  // 不删除字符
}
```

### 7.3 问题：终端不支持原始模式

**现象**：`MakeRaw` 失败

**解决方案**：
- 回退到普通输入模式（不显示 placeholder）
- 使用已有的 `readPasswordFallback` 模式

---

## 8. 结论

### 8.1 可行性评估

**✅ 完全可行**

**理由**：
1. 已有完整的技术基础（`readPasswordInput`）
2. 所需依赖已存在（`golang.org/x/term`）
3. 实现复杂度适中（约 150-200 行代码）
4. 可以复用现有代码和模式

### 8.2 建议

1. **推荐实现**：
   - 功能价值高（提升用户体验）
   - 实现成本适中
   - 不增加额外依赖

2. **实现策略**：
   - 分阶段实现，先完成基础功能
   - 充分测试，特别是边界情况
   - 保持向后兼容（placeholder 为可选参数）

3. **注意事项**：
   - 处理多字节字符
   - 提供回退方案
   - 保持代码简洁

---

## 9. 下一步行动

1. **创建实现计划**：详细列出需要修改的文件和函数
2. **编写测试用例**：定义测试场景和预期行为
3. **开始实现**：按照计划逐步实现功能
4. **测试验证**：在各种环境下测试功能

---

## 参考资料

- `internal/lib/prompt/input.go` - 现有实现
- `golang.org/x/term` - 终端控制库
- ANSI 转义码文档

